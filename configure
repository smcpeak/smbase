#! /bin/sh
# Automatic configuration.

cat <<EOF
This script does not do anything right now.

To build, just run 'make'.  If something goes wrong, take a look at the
configuration settings at the top of Makefile, and create a config.mk to
override any needed settings.
EOF

# EOF
