The files in this directory act as skeleton boilerplate for new files.
I have a script in my scripts repo, `empty-header`, that reads them, and
a command in my editor that I run to insert their contents into the
current editing buffer.
