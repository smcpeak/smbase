// typ.h            see license.txt for copyright and terms of use
// Old file that can no longer be used because it has #error.

#ifndef SMBASE_TYP_H
#define SMBASE_TYP_H

/* Substitutes:

    sm-macros.h has the macros that were here.

    div-up.h has the `div_up` function template.

    Uses of `SWAP` should be changed to `std::swap`, which can be
    obtained from sm-swap.h or <utility>.

    Everything else should come from a standard header.
*/
#error "This file is not to be used anymore.  See comments for substitutes."

#endif // SMBASE_TYP_H
